import '../web-components';

document.addEventListener('DOMContentLoaded', () => {
    // The main purpose of this file in dev mode is to ensure
    // that the web components are registered.
    // The nav.js file handles the iframe navigation.
});
