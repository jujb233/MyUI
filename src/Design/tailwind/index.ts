// This file brings in static Tailwind CSS assets used by the library.
// Importing the CSS here ensures bundlers include the styles when `src/Design` is imported.

import '../Styles/myui/variants.css'

// Export a simple marker so consumers can import from the module if needed
export const tailwindStylesIncluded = true
